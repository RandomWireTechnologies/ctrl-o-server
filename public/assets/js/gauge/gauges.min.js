!function (document, window, $) {
    "use strict";
    var Gaugedynamic = $("#GaugeDynamic").data("gauge");
    setInterval(function () {
        var random = Math.round(1e3 * Math.random()),
                options = {
                    colorStart: "#36A9E1",
                    colorStop: "#36A9E1",
                    strokeColor: '#E0E0E0'
                };
        random > 700 ? (options.colorStart = "#e91e63", options.colorStop = "#e91e63") : 300 > random && (options.colorStart = "#4caf50", options.colorStop = "#4caf50"), Gaugedynamic.setOptions(options).set(random)
    }, 1500)
    var Donutdynamic = $("#DonutDynamic").data("donut");
    setInterval(function () {
        var random = Math.round(1e3 * Math.random()),
                options = {
                    colorStart: "#36A9E1",
                    colorStop: "#36A9E1",
                    strokeColor: '#E0E0E0'
                };
        random > 700 ? (options.colorStart = "#e91e63", options.colorStop = "#e91e63") : 300 > random && (options.colorStart = "#4caf50", options.colorStop = "#4caf50"), Donutdynamic.setOptions(options).set(random)
    }, 1500)

}(document, window, jQuery);