!function (window, document, $) {
    "use strict";
    var $table = $('#floattheadtable1');
    $table.floatThead({
        top: function () {
            return $(".main-header").outerHeight()
        },
        position: "absolute",
        responsiveContainer: function ($table) {
            return $table.closest('.floatThead-wrapper');
        }
    });

    var $table = $('#floattheadtable2');
    $table.floatThead({
        top: function () {
            return $(".main-header").outerHeight()
        },
        position: "absolute",
        responsiveContainer: function ($table) {
            return $table.closest('.floatThead-wrapper2');
        }
    });
}(window, document, jQuery);