$(document).on("change", '[data-role="task"]', function () {
    "use strict";
    var $list = $(this),
    $checkbox = $list.find('[type="checkbox"]');
    $checkbox.is(":checked") ? $list.addClass("task-complated") : $list.removeClass("task-complated")
}), $('[data-role="task"]').trigger("change")
   